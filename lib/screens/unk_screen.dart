import 'package:flutter/material.dart';

class UnkScreen extends StatelessWidget {
  const UnkScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Screen Unk'),
      ),
      body: Text('Screen Unk'),
    );
  }
}
