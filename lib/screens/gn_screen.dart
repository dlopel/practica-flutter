import 'package:flutter/material.dart';

class GNScreen extends StatelessWidget {
  const GNScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Screen Gn'),
      ),
      body: Text('Screen Gn'),
    );
  }
}
